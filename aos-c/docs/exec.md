# 執行行為

一筆指令就是一次「開一個行程」的完整描述。這份文件說明每個欄位在執行時到底做了
什麼、什麼情況算失敗、以及失敗之後會發生什麼。

格式本身（八行怎麼排、Tab 怎麼分）看 [format.md](format.md)。

## 一句話版本

拿到一筆指令之後，會依序做這些事：

1. 讀環境變數檔（如果有指定）
2. `fork` 出子行程
3. 在子行程裡：開重導向的檔案、切工作目錄、換環境變數
4. 在子行程裡：`execvp` 換成目標程式
5. 在父行程裡：等子行程結束、把結束狀態寫進檔案

任何一步失敗都會回報，而且**回報得夠細**，讓你知道是哪一步壞的。

## 每個欄位做什麼

### argv：要跑什麼

第一個引數是程式名稱，走 `PATH` 查找 —— 跟你在 shell 裡直接打那個指令的行為一致。
所以 `echo` 找得到，`./my-script` 也找得到（含斜線就不查 `PATH`）。

引數原樣傳給程式，不做任何展開。`*.txt` 就是這五個字元，不會變成一串檔名。

argv 是空的，或第一個引數是空字串，會直接被拒絕（`INVALID_ARGUMENT`），連 `fork`
都不會發生。

### stdin / stdout / stderr：重導向

| 值 | 行為 |
| --- | --- |
| 空 | **繼承**呼叫者的對應控制代碼 |
| 有路徑（stdin） | 以唯讀開啟。開不起來就是 `OPEN_STDIN_FAILED` |
| 有路徑（stdout/stderr） | 以唯寫開啟，**不存在就建立，存在就清空** |

三個各自獨立，可以只重導向其中一個。

**輸出是截斷不是追加。** 選這個是因為：追加的結果沒辦法由呼叫端還原（內容已經混在
一起了），而截斷可以靠換一個新路徑來避免。等同 shell 的 `>` 而不是 `>>`。如果你
要追加，現在的做法是自己在指令裡用 `sh -c 'cmd >> file'`。

留空是「繼承」而不是「丟到 /dev/null」，因為後者會讓輸出無聲消失，除錯時很痛苦。
真的要丟掉就明確寫 `/dev/null`。

### cwd：工作目錄

留空就繼承呼叫者的工作目錄。有值就在子行程裡 `chdir` 過去，切不過去是
`CHDIR_FAILED`。

注意順序：**重導向的檔案是在 `chdir` 之前開的**，所以重導向路徑裡的相對路徑是相對
於「呼叫者的」工作目錄，不是 `cwd` 欄位指定的那個。

### env：環境變數

| 值 | 行為 |
| --- | --- |
| 空 | 子行程**完整繼承**呼叫者的環境變數 |
| 有路徑 | 讀那個檔案，用它**取代**整個環境 |

**取代，不是擴充。** 指定了 env 檔之後，呼叫者原本的環境變數一個都不會傳下去 ——
包括 `PATH`、`HOME`、`LANG`。這是刻意的：取代能做出可重現的執行環境，而「擴充」
可以由你自己在檔案裡把要保留的變數寫進去來達成；反過來卻做不到。

如果你的指令需要 `PATH`，記得在檔案裡自己寫一行。

環境變數檔的格式：

```text
# 井字號開頭是註解
                          ← 空行會被略過

PATH=/usr/bin:/bin
LANG=zh_TW.UTF-8
EMPTY=                    ← 值可以是空的
```

規則：

- 一行一個 `KEY=VALUE`
- 空行略過，`#` 開頭的行是註解
- **其他任何沒有 `=` 的行都是錯誤**（`ENV_FILE_FAILED`），不會猜你的意思
- 值裡面可以有 `=`，只有第一個 `=` 算分隔
- 不做引號處理，不做變數展開 —— 跟 argv 一樣，所有字元都是字面值

檔案讀不到、或某行不合格式，都是 `ENV_FILE_FAILED`。這個檢查是在 `fork` **之前**
做的，所以壞掉的 env 檔不會生出一個註定失敗的子行程。

### exit：把結束狀態記下來

| 值 | 行為 |
| --- | --- |
| 空 | 結束狀態不記錄，只透過回傳值給你 |
| 有路徑 | 檔案被清空，然後寫入十進位的狀態值加一個換行 |

寫進去的數字是這樣決定的：

- 正常結束 → 程式的結束碼（0 到 255）
- 被訊號終止 → `128 + 訊號編號`

第二條沿用 shell 的慣例。所以被 `SIGTERM`（15）殺掉會記成 `143`，被 `SIGKILL`
（9）殺掉會記成 `137`。這樣做的好處是這個欄位永遠只是一個數字，不需要另一種編碼；
壞處是理論上分不清「被訊號 15 殺掉」和「程式自己 exit(143)」。如果你在意這個差別，
C API 的 `aos_exec_result.signalled` 欄位會告訴你。

### extra：保留

目前完全不使用。留著是為了以後加東西時不用改格式版本。

## 什麼算失敗

這是最需要講清楚的部分。**「子行程回傳非零」不是失敗。**

| 情況 | 算什麼 | exit 檔 |
| --- | --- | --- |
| 程式跑完，回傳 0 | 成功 | 寫入 0 |
| 程式跑完，回傳 3 | **成功** | 寫入 3 |
| 程式被訊號殺掉 | **成功** | 寫入 128 + 訊號編號 |
| 找不到指令 | 失敗 | **不會被建立** |
| cwd 不存在 | 失敗 | 同上 |
| 重導向的檔案開不起來 | 失敗 | 同上 |
| env 檔壞掉 | 失敗 | 同上 |

分界線是：**程式有沒有真的跑起來。** 跑起來了，那結束碼就是資料，該記進 exit 檔而
不是當成錯誤；沒跑起來，那就是你要的事情沒發生，得讓你知道。

注意這張表講的是**單一一筆**的成敗判定。失敗之後會不會繼續跑下一筆，是
[多筆指令](#多筆指令)那一節的事 —— 答案是會。

### 「找不到指令」跟「指令回傳 127」怎麼分開的

這兩件事必須分得開，但有個技術問題：`fork` 之後子行程唯一能傳回父行程的東西就是
一個結束碼，而 shell 的慣例正好是用 127 表示「找不到指令」—— 那如果程式自己就是
回傳 127 呢？

做法是在 `fork` 之前開一個管道，並把寫入端標記成「exec 成功時自動關閉」
（`FD_CLOEXEC`）。然後：

- 子行程如果在 `execvp` **之前**的任何一步失敗（開檔、`chdir`、`execvp` 本身），
  就往管道寫進「是哪一步」和 `errno`，然後結束
- `execvp` 一旦成功，管道就被作業系統自動關閉了，什麼都沒寫進去
- 父行程讀管道：讀到資料 = 子行程沒起來；讀到 0 個位元組 = 起來了

所以父行程能百分之百分辨這兩種情況，而且因為子行程回報的是「哪一步」，才有辦法把
失敗細分成 `CHDIR_FAILED`、`OPEN_STDOUT_FAILED` 這些具體狀態，而不是一律回一個
籠統的「啟動失敗」。

## 多筆指令

用 `aos-c` 這個程式跑一個檔案時：

- 指令**依序執行**，一次一個，不併行
- 某筆的程式回傳非零，不會影響後面的指令
- **某筆執行失敗也不會停止後面的指令** —— 回報之後就跳到下一筆
- 讀取失敗要看情況，見下

會跑完全部，是因為後面的指令跟失敗的那一筆沒有關係。因為一筆跑不起來就放棄剩下的，
只是把一個失敗變成一堆沒做的事。

程式的結束碼：全部成功是 0，只要有任何一筆失敗就是 1，並在最後印出
`aos: N of M instructions failed`。全部成功時完全不輸出。

### 讀取失敗：能不能跳過要看串流還對不對得齊

| 失敗 | 能繼續嗎 | 為什麼 |
| --- | --- | --- |
| `EMPTY_ARGV` | **可以** | 整整八行都讀完才判定的，游標正好停在下一筆開頭 |
| `TOO_MANY_ARGS` | **可以** | 同上 |
| `TOO_LONG` | 不行 | 讀到一半的行就放棄了，游標停在記錄中間 |
| `INCOMPLETE` | 不行 | 串流已經沒了 |
| `READ_ERROR` | 不行 | 檔案本身壞了 |

不能繼續的時候會多印一行 `cannot resynchronise after that`，然後停止。硬讀下去只
會把後面每一筆都解成垃圾，那比停下來更糟。

### 一個格式本身的限制

**檔案中間的記錄如果被截斷，是偵測不到的。** 少了幾行，下一筆的行就會被吸收進來
補滿八行，產生一筆語法完全合法但內容全錯的指令，而且從那裡開始每一筆都錯位。只有
截斷發生在檔案**結尾**時，才會被判成 `INCOMPLETE`。

這是「八行固定、沒有分隔符號」這個格式的必然代價。要防的話得靠產生端保證寫出完整
記錄 —— `aos_instruction_write` 就是這麼設計的：整筆驗證完才開始寫。

## 狀態一覽

| 狀態 | 意思 |
| --- | --- |
| `AOS_EXEC_OK` | 程式跑完了。看 `result.status` 拿結束碼 |
| `AOS_EXEC_INVALID_ARGUMENT` | argv 是空的，或第一個引數是空字串 |
| `AOS_EXEC_OPEN_STDIN_FAILED` | stdin 的檔案開不起來 |
| `AOS_EXEC_OPEN_STDOUT_FAILED` | stdout 的檔案開不起來 |
| `AOS_EXEC_OPEN_STDERR_FAILED` | stderr 的檔案開不起來 |
| `AOS_EXEC_ENV_FILE_FAILED` | env 檔讀不到，或有一行沒有 `=` |
| `AOS_EXEC_CHDIR_FAILED` | cwd 切不過去 |
| `AOS_EXEC_SPAWN_FAILED` | `fork` 失敗，或找不到要執行的程式 |
| `AOS_EXEC_WAIT_FAILED` | 子行程起來了，但等它結束時出錯 |
| `AOS_EXEC_EXIT_WRITE_FAILED` | 程式跑完了，但結束狀態寫不進 exit 檔 |
| `AOS_EXEC_PLATFORM_UNSUPPORTED` | 這個平台還沒有實作行程建立 |

失敗時 `result.error` 會帶著作業系統回報的 `errno`，可以用 `strerror()` 轉成訊息 ——
這是「找不到指令」（`ENOENT`）和「沒有執行權限」（`EACCES`）的差別所在。

## 平台

POSIX（Linux、macOS）已經實作，用的是 `fork` / `execvp` / `dup2` / `chdir` /
`waitpid`。

Windows **還沒有**。它需要 `CreateProcess` 搭配 `STARTUPINFO` 做控制代碼重導向，
而且沒有 `fork`，所以那會是另一套實作而不是這一套的變體。在補上之前，
`aos_instruction_execute` 在 Windows 上可以編譯，但會直接回傳
`AOS_EXEC_PLATFORM_UNSUPPORTED` —— 寧可明說不支援，也不要給出會誤導的成功。

平台相關的程式碼全部關在 `src/exec.cpp` 裡面，公開標頭沒有任何平台差異。

## 安全性

**執行一個指令檔，等於用你的權限跑任意指令。**

這是這個工具的用途而不是缺陷，但意思是：**對指令檔的寫入權限等同於任意程式碼執行
權限**。如果指令檔放在別人寫得到的地方，那個人就能用你的身分做任何事。

單筆記錄有位元組預算，是為了擋住畸形檔案把記憶體吃光，但那只防資源耗盡，**不防
內容惡意**。內容的信任問題只能靠檔案權限解決。

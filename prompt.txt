Role & Task:
Act as a Senior C++ Architect. I need a single-header, zero-heavy-dependency C++17 (or C++20) LiteLLM/OpenAI API Client.

Requirements & Architecture Specs:
1. Dependencies: Only use `libcurl` and `nlohmann/json`. Must be header-only (or clear `.hpp` + `.cpp`).
2. Protocol: OpenAI Chat Completions API protocol (Targeting a local LiteLLM Proxy endpoint like `http://localhost:4000/v1`).
3. Stream Strategy: Explicitly Event-Driven Handler / Callback pattern. NO blocking generator or polling queue.

Features to Support:
- Text Generation
- System Prompt
- Multimodal Image Prompting (Base64 and Image URL inputs)
- Tools / Function Calling Payload & Response Handling
- Reasoning Content Stream Parsing (e.g. DeepSeek-R1 `<think>` tags or `reasoning_content` field in SSE delta)
- Model Capability Verification (GET `/v1/models` endpoint wrapper)

Handler Design Spec:
Please wrap handlers in a clean struct or class interface like:
```cpp
struct LLMStreamHandler {
    std::function<void(const std::string& token)> on_text;
    std::function<void(const std::string& think)> on_thinking;
    std::function<void(const nlohmann::json& tool_call)> on_tool_call;
    std::function<void()> on_complete;
    std::function<void(const std::string& error)> on_error;
};
Parsing Logic Rules:

SSE stream chunk MUST accurately clean data:  line prefixes.

Check delta.contains("reasoning_content") to trigger on_thinking.

Check delta.contains("content") to trigger on_text.

Check delta.contains("tool_calls") to trigger on_tool_call.

Stream termination when data: [DONE] triggers on_complete.

Deliverables:

Provide the complete LiteLLMClient.hpp implementation.

Provide a clean main.cpp usage example demonstrating:

Listing models.

Sending a stream request with system prompt, image prompt, and function calling tools.

Registering and executing all stream handlers.

Keep the code robust, production-ready, memory-safe, and well-commented.
